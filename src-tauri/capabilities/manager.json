{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "manager",
  "description": "Native access required by the PartyPaste manager window.",
  "windows": ["manager"],
  "permissions": [
    "core:default",
    "core:event:default",
    "manager-commands",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
