{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Least-privilege native access for the compact phrase overlay.",
  "windows": ["overlay"],
  "permissions": [
    "core:default",
    "core:event:default",
    "overlay-commands",
    "overlay-window-commands",
    "core:window:allow-start-dragging"
  ]
}
